{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'echo \"$CLAUDE_BASH_COMMAND\" | grep -qE \"git (commit|push)\" && npm run check'"
          }
        ]
      }
    ]
  }
}