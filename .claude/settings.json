{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'if echo \"$CLAUDE_BASH_COMMAND\" | grep -qE \"git (commit|push)\"; then npm run check; fi'"
          }
        ]
      }
    ]
  }
}